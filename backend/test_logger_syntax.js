const { logAudit } = require('./utils/logger');

// Since stripSensitive is internal, I'll temporarily export it or just test the outcome if I could.
// Actually, I'll just check if the code runs without syntax errors.

console.log("Logger utility loaded successfully.");
